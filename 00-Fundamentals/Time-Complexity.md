# Time Complexity

It tells how fast your code runs.

---

##  Types:

- O(1) → instant
- O(n) → one loop
- O(n²) → nested loops (slow)

---

##  Example:

```cpp
for(int i=0;i<n;i++){
    cout<<i;
}

